import ResourcesClient from "./ResourcesClient";

export const metadata = {
  title: "Free Resources — Guides, Checklists & Templates | Coach Sandeep Jadav",
  description:
    "Six free guides — SAT strategy, scholarships, application checklists, essay templates, country guides, and visas — the same frameworks our mentors use with paying families.",
};

export default function ResourcesPage() {
  return <ResourcesClient />;
}
