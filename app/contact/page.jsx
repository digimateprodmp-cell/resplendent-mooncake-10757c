import ContactClient from "./ContactClient";

export const metadata = {
  title: "Book A Strategy Session — Coach Sandeep Jadav",
  description:
    "A 45-minute strategy session with Coach Sandeep: your child's current position, global potential, and the exact roadmap between the two. No obligation.",
};

export default function ContactPage() {
  return <ContactClient />;
}
